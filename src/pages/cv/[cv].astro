---
import Resume from "../../layouts/Resume.astro";

// https://www.uuidgenerator.net/, or maybe just sequential is ok

export function getStaticPaths() {
  return [];
}

const { id } = Astro.params;
---

<Resume cv={id} skills={[]} professionalExperiences={[]} />
